version: 0.2
phases:
  install:
    runtime-versions:
      node: 22
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run
  post_build:
    commands:
      - echo "Build has been completed on `date`"