version: 0.2
phases:
  install:
    runtime-versions:
      node: 22
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - echo "Build completed on `date`"
artifacts: